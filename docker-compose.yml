version: '3.8'

services:
  influencia-app:
    build: .
    container_name: area-influencia-service
    ports:
      - "3006:3006"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: always
